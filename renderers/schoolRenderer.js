/**
 * Generates HTML for the projects section.
 * @param {Array} projects - An array of project objects fetched from Sanity.
 */
export function renderSchool(projects) {
    if (1) {
        return "<p>Do School</p>";
    }
}