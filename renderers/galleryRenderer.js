/**
 * Generates HTML for the projects section.
 * @param {Array} skills - An array of skill objects fetched from Sanity.
 * @param {Array} certifications - An array of certification objects fetched from Sanity.
 */
export function renderGallery(skills, certifications) {
    if (1) {
        return "<p>Do Gallery</p>";
    }
}